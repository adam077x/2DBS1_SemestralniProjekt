import LoginForm from '@/components/login/LoginForm';
import { Stack } from '@mui/material';

const Login = () => {
  return (
    <Stack>
      <LoginForm />
    </Stack>
  );
};

export default Login;
